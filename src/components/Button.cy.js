import { mount } from "@cypress/vue";
import Button from "./Button.vue";
describe("Button.vue", () => {
  it("第一个测试", () => {
    // mount(Button);
    console.log("1213");
    console.log(Cypress.vue);
  });
});
